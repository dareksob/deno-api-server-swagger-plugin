{
  "name": "deno-api-server-swagger-plugin",
  "version": "0.0.7",
  "description": "swagger plugin for deno api server",
  "repository": "git@github.com:dareksob/deno-api-server-swagger-plugin.git",
  "author": "Darius Sobczak",
  "license": "MIT",
  "private": false,
  "scripts": {
    "preversion": "git pull && git fetch",
    "postversion": "git push --tag && git push",
    "output-test": "deno run --allow-write tests/output-test.ts"
  }
}
